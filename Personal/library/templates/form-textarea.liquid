{% block title %}{{ model.title }} : {{ global.site_title }}{% endblock %}
{% block content %}
<section class="col-24">
	<h1>{{ model.title }}</h1>	
</section>

<section class="col-24">
	<section class="col-8 first">

		<div class="js-mul-tabs-example mul-tabs">
			{% include "page-tabs" %}
			<div id="tabs-1">
				<p>A Text Area is a form input field that gives the user the ability to enter a large amount of text.</p>
			</div>
			<div id="tabs-2">
				<p>Use the Text Area element when a user is required to enter a large amount of text on a form. For shorter content, use the <a href="form-input">Text Input</a> element.</p>
			</div>
			<div id="tabs-3">
				<ul>
					<li>Text Area should default to the Attention/Highlight border color when the user clicks inside the field.</li>
					<li>Text Area height size will be adaptable to the user’s content needs. For example, the box will increase in height as the user enters more and more content.</li>
					<li>The Input Label should always be in Title Case.</li>
				</ul>
			</div>
			<div id="tabs-4">
				<p>To use the expanding textarea example you must include the Javascript snippet listed in the "Get Code" section and also place this <code>js-mul-textarea-expand</code> class name on the textarea.</p>

				<h5>Cross browser compatibility</h5>
				<p>IE9 doesn't support rounded corners on inputs, so we square the corners of the inputs so it looks just like IE8.</p>
			</div>
		</div>

		<div class="col-24">
			<h4 class="mul-hdr-rule">Dependencies</h4>
			<table class="mul-table mul-table-borders" id="" border="0" cellpadding="0" cellspacing="0" title="" summary="">
				<thead>
					<tr>
						<th width="" scope="col">Library</th>
						<th width="" scope="col">Location or Download</th>
					</tr>
				</thead>
				<tbody>
					<tr>
						<td>jQuery 1.10.2</td>
						<td>assets/scripts/core_v2/lib/jquery</td>
					</tr>
				</tbody>
			</table>
		</div>

	</section>
	<section class="col-16">
		<h4 class="mul-hdr-rule">Default Text Area</h4>

		<!-- BASIC TEXTAREA - EXAMPLE - BEGINS -->
		<div class="col-24">
			<div class="col-16 first">
				<form class="mul-form" id="" name="" method="" action="" title="">
					<fieldset>
						<ol>
							<li>
								<label for="">Label for Text Area Goes Here:</label>
								<textarea></textarea>
							</li>
							<li class="mul-form-action"></li>
						</ol>
					</fieldset>
				</form>
			</div>
		</div>
		<!-- BASIC TEXTAREA - EXAMPLE - ENDS -->

		<h4 class="mul-hdr-rule">Expanding Text Area</h4>
		<p>The textarea will continue to increase in height as the user types more content into it.</p>
		
		<!-- EXPANDING TYPE TEXTAREA - EXAMPLE - BEGINS -->
		<div class="col-24">
			<div class="col-16 first">		
				<form class="mul-form" id="" name="" method="" action="" title="">
					<fieldset>
						<ol>
							<li>
								<label for="">Label for Text Area Goes Here:</label>
								<textarea class="js-mul-textarea-expand"></textarea>
							</li>
							<li class="mul-form-action"></li>
						</ol>
					</fieldset>
				</form>
			</div>
		</div>
		<!-- EXPANDING TYPE TEXTAREA - EXAMPLE - ENDS -->

		<!-- GET CODE - BEGINS
		<div class="col-24">
			<div class="col-24 get-code-btn-ctn">
				{% include "get-code-button" %}
			</div>

			<div class="col-24 js-mul-toggle-content mul-display-none" id="div1">

				<h4 class="mul-hdr-rule">HTML</h4>
				<h5>Default</h5>

<pre class="prettyprint linenums">
&lt;textarea class=&quot;js-mul-textarea-expand&quot;&gt;&lt;/textarea&gt;
</pre>

				<h5>Expanding</h5>
<pre class="prettyprint linenums">
&lt;textarea class=&quot;js-mul-textarea-expand&quot;&gt;&lt;/textarea&gt;
</pre>
				<h4 class="mul-hdr-rule">JS</h4>
				<h5>Expanding</h5>
				<p></p>

<pre class="prettyprint linenums">
jQuery('.js-mul-textarea-expand').textareaExpander();
</pre>

			</div>

		</div>
		GET CODE - ENDS -->

	</section>
</section>
{% endblock %}