{% block title %}{{ model.title }} : {{ global.site_title }}{% endblock %}
{% block content %}
<section class="col-24">
	<h1>{{ model.title }}</h1>	
</section>

<section class="col-24">
	<section class="col-8 first">

		<div class="js-mul-tabs-example mul-tabs">
			{% include "page-tabs" %}
			<div id="tabs-1">
				<p>The Subheader Row is a Data Table Add-On. It allows you to subdivide and call out groups of data on a Data Table.</p>
			</div>
			<div id="tabs-2">
				<p>Use the Subheader Row when you want to group data within a Data Table together.</p>
			</div>
			<div id="tabs-3">
				<strong>Color</strong>
				<p>Subheader Rows should default to the color #e8f0s7</p>
				
				<strong>Width</strong>
				<p>You can set column width using a helper Class. Since the Static Data Table functions within the Mercer Grid System, it will automatically adapt and configure itself.</p>

				<strong>Row Details</strong>
				<ul>
					<li>Padding for all Static Data Table rows is set to five pixels on the top and bottom and ten pixels to the left and right.</li>
					<li>Line height of Basic Data Table Subheader Rows is 13 pixels to keep text consistent with Mercer branding.</li>
					<li>Subheader Row text is left aligned.</li>
					<li>Subheader Row text is always in bold.</li>
				</ul>
			</div>
			<div id="tabs-4">
				{% include "table-navbar" %}
			</div>
		</div>

	</section>
	<section class="col-16">

		<h4 class="mul-hdr-rule">Example</h4>

		<!-- TABLE WITH SUBHEADER - EXAMPLE - BEGINS -->
		<table class="mul-table mul-table-borders mul-table-striped mul-table-hover" id="" border="0" cellpadding="0" cellspacing="0" title="" summary="">
			<thead>
				<tr>
					<th class="col-10" scope="col">First Name</th>
					<th class="col-11" scope="col">Last Name</th>
					<th class="col-3 mul-nowrap mul-align-right" scope="col">Start Date</th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<td class="mul-subheader-row" colspan="3">Sub Header Row</td>
				</tr>
				
				{% for item in global.table_data %}
				<tr>
					<td>{{ item.first_name }}</td>
					<td>{{ item.last_name }}</td>
					<td class="mul-nowrap mul-align-right">{{ item.start_date }}</td>
				</tr>
				{% endfor %}

			</tbody>
		</table>
		<!-- TABLE WITH SUBHEADER - EXAMPLE - ENDS -->

		<!-- GET CODE - BEGINS
		<div class="col-24">
			<div class="col-24 get-code-btn-ctn">
				{% include "get-code-button" %}
			</div>

			<div class="col-24 js-mul-toggle-content mul-display-none" id="div1">

				<h4 class="mul-hdr-rule">HTML</h4>
				<p>Put any content you want in here.</p>

<pre class="prettyprint linenums">

</pre>

				<h4 class="mul-hdr-rule">JS</h4>
				<p>Put any content you want in here.</p>

<pre class="prettyprint linenums">

</pre>

			</div>

		</div>
		GET CODE - ENDS -->

	</section>
</section>
{% endblock %}