{% block title %}{{ model.title }} : {{ global.site_title }}{% endblock %}
{% block content %}
<section class="col-24">
	<h1>{{ model.title }}</h1>	
</section>

<section class="col-24">
	<section class="col-8 first">

		<div class="js-mul-tabs-example mul-tabs">
			{% include "page-tabs" %}
			<div id="tabs-1">
				<p>The Subtotal Row is a Data Table Add-On. It functions the same way the Total Row Add-On does, it’s just a different color.</p>
			</div>
			<div id="tabs-2">
				<p>Use the Subtotal Row when you want to call out a subtotal on a Data Table. You can’t use a Subtotal Row unless you also have a Total Row Add-On within a Data Table.</p>
			</div>
			<div id="tabs-3">
				<strong>Color</strong>
				<p>The Subtotal Row should default to a lighter background color than the Total Row: #e6e6e6.</p>
				
				<strong>Width</strong>
				<p>You can set the column width using a helper Class. Since the Data Table functions within the Mercer Grid System, it will automatically adapt and configure itself.</p>

				<strong>Row Details</strong>
				<ul>
					<li>Padding for all Static Data Table rows is set to five pixels on the top and bottom and ten pixels to the left and right.</li>
					<li>Line height of Static Data Table Subtotal rows is 13 pixels to keep text consistent with Mercer branding.</li>
					<li>Subtotal Row text is right aligned.</li>
					<li>Subtotal Row text is always in bold.</li>
				</ul>
			</div>
			<div id="tabs-4">
				{% include "table-navbar" %}
			</div>
		</div>

	</section>
	<section class="col-16">

		<h4 class="mul-hdr-rule">Example</h4>
		
		<!-- TABLE WITH TOTAL ROW - EXAMPLE - BEGINS -->
		<table class="mul-table mul-table-borders mul-table-striped" id="" border="0" cellpadding="0" cellspacing="0" title="" summary="">
			<thead>
				<tr>
					<th class="col-10" scope="col">First Name</th>
					<th class="col-10" scope="col">Client</th>
					<th class="col-4 mul-align-right" scope="col">Renewals</th>
				</tr>
			</thead>
			<tbody>

				{% for item in global.table_data %}
				<tr>
					<td>{{ item.first_name }}</td>
					<td>{{ item.company }}</td>
					<td class="mul-align-right">{{ item.num_of }}</td>
				</tr>
				{% endfor %}

			</tbody>
			<tfoot>
				<tr class="mul-subtotal-row">
					<td class="mul-align-right" colspan="2">Subtotal</td>
					<td class="mul-align-right">58</td>
				</tr>
				<tr class="mul-total-row">
					<td class="mul-align-right" colspan="2">Total</td>
					<td class="mul-align-right">58</td>
				</tr>
			</tfoot>
		</table>
		<!-- TABLE WITH TOTAL ROW - EXAMPLE - ENDS -->

		<!-- GET CODE - BEGINS
		<div class="col-24">
			<div class="col-24 get-code-btn-ctn">
				{% include "get-code-button" %}
			</div>

			<div class="col-24 js-mul-toggle-content mul-display-none" id="div1">

				<h4 class="mul-hdr-rule">HTML</h4>
				<p>Put any content you want in here.</p>

<pre class="prettyprint linenums">

</pre>

				<h4 class="mul-hdr-rule">JS</h4>
				<p>Put any content you want in here.</p>

<pre class="prettyprint linenums">

</pre>

			</div>

		</div>
		GET CODE - ENDS -->

	</section>
</section>
{% endblock %}