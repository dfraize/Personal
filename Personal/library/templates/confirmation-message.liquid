{% block title %}{{ model.title }} : {{ global.site_title }}{% endblock %}
{% block content %}
<section class="col-24">
	<h1>{{ model.title }}</h1>
</section>

<section class="col-24">
	<section class="col-8 first">

		<div class="js-mul-tabs-example mul-tabs">
			{% include "page-tabs" %}
			<div id="tabs-1">
				<p>
					The Confirmation Message container is intended to provide positive communication back to the user whenever that user has successfully completed an action.
				</p>
					
				<h5>Variations</h5>
				<p>
					There are three message containers that all use the same structural mark up.
				</p>

				<ul>
					<li><a href="error-message">Error Message</a></li>
					<li><a href="confirmation-message">Confirmation Message</a></li>
					<li><a href="attention-message">Attention Message</a></li>
				</ul>
			</div>
			<div id="tabs-2">
				<p>The Confirmation Message container should be displayed <strong>when the user has successfully completed an action</strong>, like updating an address or completing a task, and needs confirmation that the action was successful and no further action is needed.</p>
			</div>
			<div id="tabs-3">
				<ul>
					<li>The container will span the entire width of its parent container and be located immediately below the page title.</li>
					<li>There should only ever be one Confirmation Message container on a page at a time.</li>
					<li>
						If there are multiple confirmations that need to get communicated, the page should display a single confirmation message container with multiple messages inside the container.
					</li>
					<li>
						There are two additional types of configurations that should be supported:
						<ul>
							<li><strong>Optional Close</strong></li>
								<ul>
									<li>This configuration gives the user the ability to "close" the message container via the "close" icon in the upper right corner. When clicked, the container will dissolve and all content displayed below the container will shift up on the screen.</li>
								</ul>
							<li><strong>In Page Display vs On Top of Page Display</strong> - Each message container (Error, Attention, and Confirmation) should have a setting that will determine where the message will display.</li>
								<ul>
									<li>The default display will be as an item within the rest of the content within the screen (In Page Display). </li>
									<li>There is an alternative configuration that allows the message container to display "on top of" the other content on the page. This would be used in cases where it is highly important that the user notice and acknowledge the message. In this case, the container should be centered on the screen, near the top of the browser. </li>
								</ul>
							</li>
						</ul>
					</li>
					<li>
						Title
						<ul>
							<li>The title should always contain the "green checkmark" icon</li>
							<li>The title should always be "Confirmation"</li>
							<li>The title should never be a link</li>
						</ul>
					</li>

					<li>
						Message
						<ul>
							<li>The message should be able to support two different formats</li>
							<li><strong>Paragraph Text</strong> - This can be multiple lines or even multiple paragraphs.</li>
							<li><strong>List Items</strong> - This can be a single bulleted list item or multiple bulleted list items.</li>
						</ul>
					</li>
					<li>
						Colors
						<ul>
							<li>Confirmation Messages should adhere to the approved <a href="colors">Mercer color palette</a> for all aspects of the container: Background, Border, Text</li>
						</ul>
					</li>
				</ul>
			</div>
			<div id="tabs-4">
				<h5>Cross browser compatibility</h5>
				<p>IE9 doesn't crop background gradients on containers with rounded corners, so we square the corners of every container so it looks just like IE8.</p>
			</div>
		</div>

		<div class="col-24">
			<h4 class="mul-hdr-rule">Dependencies</h4>
			<table class="mul-table mul-table-borders" id="" border="0" cellpadding="0" cellspacing="0" title="" summary="">
				<thead>
					<tr>
						<th width="" scope="col">Library</th>
						<th width="" scope="col">Location or Download</th>
					</tr>
				</thead>
				<tbody>
					<tr>
						<td>jQuery 1.10.2</td>
						<td>assets/scripts/core_v2/lib/jquery</td>
					</tr>
				</tbody>
			</table>
		</div>

	</section>
	<section class="col-16">

		<a href="javascript:;" class="mul-float-right" onclick="location.reload();">Refresh</a>
		<h4 class="mul-hdr-rule">Default</h4>

	
		<!-- CONFIRMATION MESSAGE CONTAINER - EXAMPLE - BEGINS -->
		<div class="mul-box mul-box-rnd mul-confirmation-ctn">
			<div class="mul-box-hdr">
				<span class="mul-message-icon"></span>
				<h6 class="mul-message-hdr">Success</h6>
			</div>
			<div class="box-bdy">
				<p>Confirmation message text&hellip;</p>
			</div>
		</div>
		<!-- CONFIRMATION MESSAGE CONTAINER - EXAMPLE - ENDS -->

		<h4 class="mul-hdr-rule">With Close Action</h4>
	
		<!-- CONFIRMATION MESSAGE CONTAINER WITH CLOSE - EXAMPLE - BEGINS -->
		<div class="mul-box mul-box-rnd mul-confirmation-ctn">
			<div class="mul-box-hdr">
				<span title="Close" class="mul-close-action js-mul-close-ctn"></span>
				<span class="mul-message-icon"></span>
				<h6 class="mul-message-hdr">Success</h6>
			</div>
			<div class="box-bdy">
				<p>Confirmation message text&hellip;</p>
			</div>
		</div>
		<!-- CONFIRMATION MESSAGE CONTAINER WITH CLOSE - EXAMPLE - ENDS -->


		<!-- GET CODE - BEGINS
		<div class="col-24">
			<div class="col-24 get-code-btn-ctn">
				{% include "get-code-button" %}
			</div>

			<div class="col-24 js-mul-toggle-content mul-display-none" id="div1">

				<h4 class="mul-hdr-rule">HTML</h4>
				<h5>Default behavior</h5>

<pre class="prettyprint linenums">

</pre>

				<h5>With a close button</h5>
<pre class="prettyprint linenums">

</pre>
				<h4 class="mul-hdr-rule">JS</h4>
				<p></p>

<pre class="prettyprint linenums">

</pre>

			</div>

		</div>
		GET CODE - ENDS -->


	</section>
</section>
{% endblock %}